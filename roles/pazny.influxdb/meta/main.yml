---
galaxy_info:
  role_name: influxdb
  namespace: pazny
  author: Pázny
  description: InfluxDB 2.x time-series database in Docker compose override (devBoxNOS observability stack)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: MacOSX
      versions:
        - all

# Žádné role dependencies — InfluxDB se spouští jako součást observability
# stacku vedle Grafana / Prometheus / Loki / Tempo. Orchestrace (compose up
# sequencing) je v tasks/stacks/core-up.yml.
dependencies: []

collections: []
