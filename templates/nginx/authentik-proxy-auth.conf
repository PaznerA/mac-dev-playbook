# ==============================================================================
# Authentik Forward Auth (nginx auth_request)
# Include tento snippet v location / {} bloku služby bez nativního OIDC.
# Vyžaduje: Authentik Proxy Provider pro danou službu v Authentiku.
#
# Použití v *.conf vhostu:
#   location / {
#       include {{ homebrew_prefix }}/etc/nginx/authentik-proxy-auth.conf;
#       proxy_pass http://upstream;
#       ...
#   }
# ==============================================================================

# Forward auth request k Authentik embedded outpost
auth_request        /outpost.goauthentik.io/auth/nginx;
error_page 401 = @goauthentik_proxy_signin;

# Předat auth cookies zpět klientovi
auth_request_set $auth_cookie $upstream_http_set_cookie;
add_header Set-Cookie $auth_cookie;

# Předat autentizované hlavičky upstreamu
auth_request_set $authentik_username $upstream_http_x_authentik_username;
auth_request_set $authentik_groups $upstream_http_x_authentik_groups;
auth_request_set $authentik_email $upstream_http_x_authentik_email;
proxy_set_header X-authentik-username $authentik_username;
proxy_set_header X-authentik-groups $authentik_groups;
proxy_set_header X-authentik-email $authentik_email;
proxy_set_header Remote-User $authentik_username;
proxy_set_header Remote-Email $authentik_email;
