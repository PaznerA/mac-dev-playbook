---
# ==============================================================================
# Portainer CE – Docker management UI (Docker)
# Admin účet vytvořen automaticky přes API po prvním startu.
# ==============================================================================

- name: "[Portainer] Ensure data directory exists"
  ansible.builtin.file:
    path: "{{ portainer_data_dir }}"
    state: directory
    mode: '0755'
